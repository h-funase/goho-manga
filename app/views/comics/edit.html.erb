<div class="contents row">
  <div class="container">
    <%= form_for @comic, url: comic_path do |form| %>
      <h3>
        編集する
      </h3>
      <%= form.text_field :title, placeholder: "タイトル" %>
      <%= form.text_field :image, placeholder: "画像のURL" %>
      <%= form.text_area :text, placeholder: "紹介文" , rows: "3" %>
      <%= form.submit "送信" %>
    <% end %>
  </div>
</div>